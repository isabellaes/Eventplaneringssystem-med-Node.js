import Container from "@mui/material/Container";

const EventDetailPage = () => {
  return (
    <Container
      sx={{ backgroundColor: "white", minHeight: "100vh", minWidth: "90vw" }}
    >
      <p>hello world event detail</p>
    </Container>
  );
};

export default EventDetailPage;
