import Container from "@mui/material/Container";

const CreateEventPage = () => {
  return (
    <Container
      sx={{ backgroundColor: "white", minHeight: "100vh", minWidth: "90vw" }}
    >
      <p>hello world create</p>
    </Container>
  );
};

export default CreateEventPage;
